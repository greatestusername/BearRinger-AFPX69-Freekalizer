org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# AndroidX is required for modern Android dependencies.
android.useAndroidX=true

# Keep on until we confirm no legacy/support deps are pulled transitively.
android.enableJetifier=true
